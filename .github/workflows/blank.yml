name: CI

on:
  push:
    paths-ignore:
      - '*.md'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - name: Bootstrap
        run: npm ci

      - name: Test
        run: npm run test

      - name: Bundle
        run: npm run bundle
